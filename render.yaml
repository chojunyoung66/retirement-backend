services:
  - type: web
    name: retirement-backend
    runtime: node
    buildCommand: npm install && npm run build && npx prisma db execute --file scripts/resolve-failed-migration.sql && npx prisma migrate deploy
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: GOOGLE_CLIENT_ID
        sync: false
